query GetSavedEvents($events: [uuid!]!) {
  events(where: {id: { _in: $events}}) {
    banner_image
    description
    end_date
    id
    image
    location
    name
    start_date
    event_type {
      name
    }
    attendings_aggregate {
      aggregate {
        count
      }
    }
  }
}
